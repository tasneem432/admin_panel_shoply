import 'package:flutter/material.dart';


class ProductUnitMobile extends StatelessWidget {
  const ProductUnitMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Product Brand Mobile"),
    );
  }
}
