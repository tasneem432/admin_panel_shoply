import 'package:flutter/material.dart';

class ProductUnitTablet extends StatelessWidget {
  const ProductUnitTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Product Brand Tablet"),
    );
  }
}

