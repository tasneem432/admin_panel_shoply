import 'package:flutter/material.dart';

class ProductBrandTablet extends StatelessWidget {
  const ProductBrandTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Brand tablet "),
    );
  }
}

