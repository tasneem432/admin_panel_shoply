import 'package:flutter/material.dart';

class ProductBrandMobile extends StatelessWidget {
  const ProductBrandMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Brand mobile"),
    );
  }
}

