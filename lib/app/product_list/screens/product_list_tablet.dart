import 'package:flutter/material.dart';

class ProductListTablet extends StatelessWidget {
  const ProductListTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Product List tablet"),
    );
  }
}
