import 'package:flutter/material.dart';

class ProductListMobile extends StatelessWidget {
  const ProductListMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Product List mobile"),
    );
  }
}
